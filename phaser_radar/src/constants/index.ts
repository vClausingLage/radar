import { RadarSettings } from '../types/index'

export const radarSettings: RadarSettings = {
    range: 450,
    pulseDir: { x: 0, y: -1 },
    sensitivity: 5,
    pos: { x: 0, y: 0 },
    isScanning: false,
    aperture: 360,
  }